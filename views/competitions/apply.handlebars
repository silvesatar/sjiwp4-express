<div class="p-2">
<div class="hero min-h-screen bg-base-200">
    <div class="hero-content text-center">
        <div class="max-w-md">
            <p class="pt-3">
                Uspješno ste prijavljeni na natjecanje
            </p>
            <p class="py-3 text-xs brightness-50">
                {{message}}
            </p>
            <a href="/" class="btn btn-primary">Naslovnica</a>
        </div>
    </div>
</div>
    {{#if result.display_form}}
     <form action="/competitions/apply" method="get" class="flex flex-col gap-2"></form>
     {{/if}}
</div>